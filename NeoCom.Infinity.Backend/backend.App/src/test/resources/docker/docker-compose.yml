version: '3'
services:
  postgres-dev:
    container_name: postgres
    image: "postgres:11.2"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=neocom
      - POSTGRES_PASSWORD=01.Alpha
