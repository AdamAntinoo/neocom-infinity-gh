import { ISKNoDecimalsPipe } from './iskno-decimals.pipe';

describe('ISKNoDecimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new ISKNoDecimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
